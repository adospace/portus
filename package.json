{
  "name": "portus",
  "version": "0.1.0",
  "description": "portus — agentic terminal: mission control for multiple AI agent CLI sessions",
  "license": "MIT",
  "main": "dist/electron/main.js",
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "build:bundle": "node scripts/build.mjs",
    "build:css": "tailwindcss -i renderer/styles.css -o dist/renderer/styles.css --minify",
    "build": "npm run build:bundle && npm run build:css",
    "typecheck": "tsc --noEmit",
    "test:sidecar": "node scripts/test-sidecar.mjs",
    "sidecar:build": "dotnet publish sidecar/Portus.Sidecar.csproj -c Release -r win-x64 --self-contained -p:PublishSingleFile=true -o dist/sidecar",
    "sidecar:build:mac": "dotnet publish sidecar/Portus.Sidecar.csproj -c Release -r osx-arm64 --self-contained -p:PublishSingleFile=true -o dist/sidecar",
    "start": "npm run build && node scripts/start.mjs",
    "dev": "npm run build && node scripts/start.mjs"
  },
  "dependencies": {
    "node-pty": "^1.0.0"
  },
  "devDependencies": {
    "@iconify-json/lucide": "^1.2.0",
    "@iconify/iconify": "^3.1.1",
    "@tailwindcss/cli": "^4.0.0",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-web-links": "^0.11.0",
    "@xterm/addon-webgl": "^0.18.0",
    "@xterm/xterm": "^5.5.0",
    "electron": "^33.0.0",
    "esbuild": "^0.24.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.6.0"
  }
}
